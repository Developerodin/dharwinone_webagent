# Google Identity Services client id.
# Must be the same value as GOOGLE_CLIENT_ID in backend/.env.
# Authorized JavaScript origins in Google Cloud Console must include this app's
# origin (http://localhost:5173 in dev) or the sign-in popup will be rejected.
VITE_GOOGLE_CLIENT_ID=
